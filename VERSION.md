# 版本说明

> 版本号统一登记在本文件（项目根目录 `VERSION.md`）。
> 规则：每次发布/功能改动/重要修复，在这里递增版本号并补充一行历史。

## 当前版本

**0.9.0 beta**

- 状态：beta
- 日期：2026-08-20
- 内容：**右键菜单完整交互（删除/复制/改名/粘贴）+ 按键改名与显示名体系 + 内置默认布局更新（Tab 56×48）+ 子菜单窗口自适应 + 整体动画 + 关于页链接（伴生进程通道）+ 主题调色盘修复 + 浮层派生配色**——
  1. **右键菜单重做**：右键按键 = 删除（确认框）/ 复制 / 修改显示名；右键鼠标垫 = 隐藏鼠标垫；右键空白处 = 粘贴（剪贴板非空时）+ 显示鼠标垫（垫隐藏时）——无可用项则不弹菜单；
  2. **按键改名**：显示名持久化（重启不丢）；显示名自动剥离 "(n)" 副本后缀（Space(2) 显示为「空格」）；「重置按键布局」同时恢复默认显示名；
  3. **复制/粘贴**：复制按键（含显示名与尺寸）→ 空白处右键粘贴，重名自动命名 "名(2)""(3)"（VK 映射不受影响）；粘贴位置 = 鼠标点击处；
  4. **内置默认布局更新**：以用户实际配置为准（数据区/默认.json）——Tab 键默认尺寸 **56×48**、鼠标垫位置 (94,0) 与尺寸 223.6×139.8；旧版改名宽度自适应污染的 Tab 尺寸（68/74）启动/保存/应用预设时自动修复；
  5. **子菜单窗口自适应**：自定义控件/主题颜色/调色盘内容可滚动（窗口矮时功能不被裁）；宽度随窗口收缩；窄窗下调色盘自动叠放不溢出；
  6. **整体动画（第一阶段）**：全部弹层淡入（160ms）+ 启动整体淡入（320ms），CubicEase 缓出；
  7. **关于页链接**：优先经伴生进程打开浏览器（管道新增 OPEN_URL 命令，桌面进程绕开沙箱）；失败自动复制链接 + 页内提示；链接文字可选中复制；
  8. **主题调色盘修复**：拖动色相条立即出纯色（无色相时自动补满饱和度）；松手不再切回红色；白/黑/灰等无色相颜色保留当前色相（盘不跳红）；
  9. **浮层派生配色**：右键菜单 / 改名面板 / 删除确认框按面板亮度自适应派生（深底提亮、浅底压暗、边框再外扩一档），与主界面拉开层次；
  10. **遮罩透明化**：设置/锁定/预设/颜色等覆盖层不再叠加变深（点外部关闭保留）；
  11. **其他修复**：改名面板补主题背板；重置布局后按键名/配色完整恢复（不再黑字透明底）；自定义控件菜单打开时重置布局按键正常显示；移除锁定菜单关闭按钮；鼠标垫悬停恢复缩放光标（防 GC 回收消失）；色相条/透明度条/SV 块拖动不被滚动容器抢占（ManipulationMode）。
- 任务书：无（0.8.0 后的迭代测试构建 1.6.0.0 → 1.20.0.0 收敛）；上一版：0.8.0 beta（已发布）。

## 版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| 0.9.0 beta | 2026-08-20 | 右键菜单完整交互（删除/复制/改名/粘贴）+ 按键改名与显示名体系（重置恢复/预设携带）+ 内置默认布局更新（Tab 56×48 + 鼠标垫 94,0/223.6×139.8 + 尺寸污染自动修复）+ 子菜单窗口自适应（滚动/缩宽/叠放）+ 整体动画（弹层淡入/启动淡入）+ 关于页链接（伴生进程 OPEN_URL 通道 + 剪贴板兜底）+ 主题调色盘修复（拖色相出纯色/不再切回红）+ 浮层派生配色（右键菜单/改名面板/删除确认框）+ 遮罩透明化 + 改名面板背板/重置恢复配色/光标修复等（UWP 包 1.20.0.0） |
| 0.7.0 beta | 2026-08-19 | 用户预设（主题/布局，包外 presets.json 卸载不丢）+ 自定义按键新增鼠标/滚轮键（滚轮 150ms 点亮）+ 鼠标垫右侧滚轮键 + 显示名统一（左Shift/左Ctrl/左Alt/空格）+ 预设子菜单简化 + 配色按键底联动/调色编号实时同步修复 + bar 内透明度修复 + 安装升级优先不丢数据（UWP 包 1.4.0.0） |
| 0.5.2 beta | 2026-08-18 | 设置按钮→正常按钮「设置」+ 鼠标垫显示/隐藏开关（持久化+隐藏≠删除）+ 删触摸板模板 + 自带键可删除（重置兜底）（UWP 包 1.2.2.0） |
| 0.5.1 beta | 2026-08-18 | 移动挤压修复：移动改用 RenderTransform（不挤压兄弟按键）+ 持久化 transform + 缩放归一（UWP 包 1.2.1.0） |
| 0.5.0 beta | 2026-08-18 | 全局参考线吸附（隔空对齐+距离分级参考线）+ 鼠标垫长按移动/等比缩放（自动跟随开关+重置恢复）+ 触摸板控件 + 文字调整（UWP 包 1.2.0.0） |
| 0.4.1 beta | 2026-08-17 | 安装器紧急修复：升级残留 bug + 防强制重启 + 中文化 + 编码根治（UWP 包 1.1.0.0） |
| 0.4.0 beta | 2026-08-17 | 自定义控件（87 配列按键添加器）：设置面板精简 + 长按移动 + 右键删除 + 折叠键盘选择器 + 协议 v3 + 窗口 +1/3 + 重置恢复出厂 + 锁定时不可移动 |
| 0.3.1 beta | 2026-08-17 | 按键边缘悬停光标反馈（Size 光标）+ 同键同模式去重修复 + CaptureLost 光标兜底 |
| 0.3.0 beta | 2026-08-17 | 自定义布局（拖拽缩放+锁定开关+持久化）、设置新增"重置按键布局"按钮、修复拖拽释放闪退 |
| 0.2.0 beta | 2026-08-17 | 新增设置子菜单 + 测试按钮（点击反馈）+ 日志链路 |
| 0.1.0 beta | 2026-08-16 | 小版本升级：0.0.x 起步阶段收敛（功能稳定） |
| 0.0.4 beta | 2026-08-16 | 修复累计浮点导致泵崩溃（光标/按键冻结） |
| 0.0.3 beta | 2026-08-16 | 游戏内校准缩放 + 屏幕钳制（修复垫子"不够用"/顶边漂移） |
| 0.0.2 beta | 2026-08-16 | 鼠标点平滑跟手（指数插值，Bongo Cat 同款逻辑） |
| 0.0.1 beta | 2026-08-16 | 起步版本：首个统一版本号（涵盖此前全部功能与修复） |

## 版本号命名规则

- 采用语义化版本：`主版本.次版本.修订号` + 可选后缀（`beta` / `rc` / 正式版不带后缀）。
- 起步阶段（0.x）：功能或行为有较大变化 → 递增次版本；小修小改 → 递增修订号。

## 发布规则（每次版本更新必须同时包含以下两项）

1. **源代码**：`git commit` 提交本次全部改动（含 `VERSION.md` 版本登记）；
2. **安装包**：重建 `Setup.exe`（`ISCC installer\setup.iss`），产物
   `dist\KeySetup.Setup\KeyDisplaySetup.exe`，并同步刷新桌面备份副本。

**安装包归档（保证"每版安装包 ↔ 源码"对应）**：

- 每个版本发布时，把安装包复制到 `release\<版本>\KeyDisplaySetup.exe` **并随源码一起提交进 git**
  （`release/` 不在 .gitignore 内）；版本号与 `VERSION.md`、`installer\setup.iss` 的 `MyAppVersion` 三者保持一致。
- 这样每个版本的安装包与源码在同一次提交里，天然一一对应，历史版本可随时取用。

> 即：版本递增（发布）时，源码与安装包必须成对交付，缺一不可。
> 仅文档/说明类改动不递增版本，不强制重建安装包。
