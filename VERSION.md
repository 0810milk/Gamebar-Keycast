# 版本说明

> 版本号统一登记在本文件（项目根目录 `VERSION.md`）。
> 规则：每次发布/功能改动/重要修复，在这里递增版本号并补充一行历史。

## 当前版本

**0.0.1 beta（起步版本）**

- 状态：起步版本（beta）
- 日期：2026-08-16
- 内容：Game Bar 按键显示组件核心功能可用 —— 键盘/鼠标状态实时显示、固定后只留按键、
  黑白主题切换、高帧率渲染（跟随显示器刷新率）、鼠标垫按屏幕比例缩放、
  多客户端管道（多实例可同时连接）、输入检测 500Hz 限频、日志监控（原生输入/坐标链路/渲染状态）。

## 版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| 0.0.1 beta | 2026-08-16 | 起步版本：首个统一版本号（涵盖此前全部功能与修复） |

## 版本号命名规则

- 采用语义化版本：`主版本.次版本.修订号` + 可选后缀（`beta` / `rc` / 正式版不带后缀）。
- 起步阶段（0.x）：功能或行为有较大变化 → 递增次版本；小修小改 → 递增修订号。
